<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BTCK_LHD_113">

        <activity
            android:name=".ui.KhoidongActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BTCK_LHD_113">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.DangnhapActivity" android:exported="false" />
        <activity android:name=".ui.DangkyActivity" />
        <activity android:name=".ui.TrangchuActivity" />
        <activity android:name=".ui.ChitietbaihocActivity" />
        <activity android:name=".ui.HosoActivity" />
        <activity android:name=".ui.DoiMatKhauActivity" />
        <activity android:name=".ui.HocTuVungActivity" />
        <activity android:name=".ui.TracNghiemActivity" />
        <activity android:name=".ui.TienDoActivity" />
        <activity android:name=".ui.KetQuaActivity" />

    </application>

</manifest>
